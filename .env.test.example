# Test suite -> Postgres lokal (lihat docs/TESTING.md). JANGAN pakai string Neon di sini.
DATABASE_URL=postgresql://egg:egg@localhost:54323/egg_os_test

# R2 credentials placeholder — test PAKAI mock signer via DI, kredensial ini nol dipakai runtime.
# Owner harus generate real R2 API token di CF dashboard untuk staging/prod (JANGAN commit yang asli).
R2_ACCOUNT_ID=dummy-account-id-for-test
R2_ACCESS_KEY_ID=dummy-access-key-for-test
R2_SECRET_ACCESS_KEY=dummy-secret-key-for-test
